extends layout

mixin canvas(canvas)
	li.canvas
		canvas(width="300", height="300", class!=attributes.class, data-canvas=canvas)
		if canvas && canvas.name
			#byline
				small.text-muted By #{canvas.name}

block content
	// Draw
	.container
		.page-header
			h1 Draw&nbsp;
				small just because
		#alerts(data-error=error)
			.error.alert.alert-danger
				strong.message
			.success.alert.alert-success
				strong.message
		#tools.btn-toolbar(role="toolbar")
			.btn-group 
				button#tool-clear.btn.btn-default(type="button", data-toggle="tooltip", data-placement="top", title="Clear the canvas")
					span.glyphicon.glyphicon-fire
				button#tool-play.btn.btn-default(type="button", data-toggle="tooltip", data-placement="top", title="Play your drawing")
					span.glyphicon.glyphicon-play-circle
			if canvases.length
				.btn-group
					button#add.btn.btn-primary(type="button", title="Add to this Drawing") Add to this Drawing!
		ul#canvases.list-inline
			if canvases.length
				each canvas in canvases
					+canvas(canvas)
				+canvas(null)(class="template")
			else
				+canvas(null)
		#actions(role="actions")
			.input-group
				input#name.form-control(type="text", placeholder="Arteest Name")
				span.input-group-btn
					button#save.btn.btn-primary(type="button", title="Fin!") Fin!